# dsh-pet-bridge 验收判定报告

> 判定者：独立验收裁判（仅依据 BRIEF / TEST-PLAN / test-output 三份文件）
> 判定日期：本会话
> 范围声明：只读取 `.devflow/BRIEF.md`、`.devflow/TEST-PLAN.md`、`.devflow/test-output.txt` 三份文件，未查看其他项目文件、git 历史或 .devflow 其他产物。

---

## 一、逐条判定表

对照 TEST-PLAN 的 26 条编号场景 + 2 条跳过场景，逐条对到 test-output 的测试用例。

### 一、正常情况

| # | 清单场景 | 判定 | 依据测试用例 |
|---|---------|------|------------|
| 1 | 回合开始 → 「dsh·思考中」user 类 | 过 | `turn/start 推 kind=user、agent_source="dsh"、tool_name=null` |
| 2 | 开始跑工具 → 中文文案，`pre` 类 | 过 | `tool/call 推 kind=pre，tool_name=中文文案，tool_input 恒 null` |
| 3 | 工具跑完 → `post` 推送 | 过 | `tool/result 推 kind=post，tool_name=null、tool_input=null` |
| 4 | 回合结束 → `stop` 推送 | 过 | `turn/end 推 kind=stop，tool_name=null、tool_input=null` |
| 5 | 模型自言自语 → 气泡完全不动 | 过 | `assistant/message 不推送（喂入后等待，0 次推送）` |
| 6 | 完整一回顺序走 思考中→工具名→完成 | 过 | `一个完整回合：start→call→result→end 依次外发 user/pre/post/stop` |
| 7 | 气泡身份：叫 dsh；sender 是本机 dsh pid；只发本机端口 | **部分过** | 身份 `agent_source="dsh"` 已验（turn/start 用例）；但 **caller_pid 填充、真实回环 127.0.0.1 绑定 未在输出中被显式断言** —— 测试用临时端口转发，未真连 7779，pid/回环不回环属接口层无法验证项（见「存疑项」） |

### 二、工具名中文映射

| # | 清单场景 | 判定 | 依据测试用例 |
|---|---------|------|------------|
| 8 | 读/搜类 → 「读取中」 | 过 | `读取类：read/grep/glob/list 前缀 → 读取中`；`_search 结尾（词边界）→ 读取中` |
| 9 | 写文件类 → 「写入文件」 | 过 | `写入类：write/edit/apply_patch/insert → 写入文件` |
| 10 | 命令类 → 「运行命令」 | 过 | `命令类：bash/exec/run/command/shell → 运行命令` |
| 11 | 联网类 → 「联网检索」 | 过 | `联网类：web_search/http/fetch/request/curl → 联网检索` |
| 12 | 编排类 → 「编排任务」 | 过 | `编排类：subagent/agent/workflow/skill → 编排任务` |
| 13 | 没见过的工具 → 「执行中」 | 过 | `未知工具名（frobnicateX）→ 兜底执行中` |

### 三、边界和怪输入

| # | 清单场景 | 判定 | 依据测试用例 |
|---|---------|------|------------|
| 14 | 沾边但不中（supersearch 等）→ 执行中 | 过 | `反向：supersearch/nonsearch/doesearch 不得命中搜索类`；`part 命中边界内才命中` |
| 15 | 超长参数不外发 | 过 | `超长 arguments：不外发，payload 不出现参数内容` + `tool_input 恒 null` |
| 16 | enabled=false 零推送 | 过 | `enabled=false：正常装载、零推送（有事件也不发）` |
| 17 | 会话没事件不崩 | 过 | `空 events 数组：不崩、无推送`；`session.events 缺失（undefined）：跳过、不崩` |
| 18 | 工具缺名字 → 执行中 | 过 | `tool/call 缺 data.name（undefined）：tool_name 兜底为执行中，恒非空字符串` |

### 四、宠物挂掉 / 拒绝

| # | 清单场景 | 判定 | 依据测试用例 |
|---|---------|------|------------|
| 19 | 端口没人听：dsh 不受影响、不重试 | 过 | `推送目标无监听（ECONNREFUSED）：不抛异常、不重试、后续事件仍处理` |
| 20 | 宠物返回错误码：静默降级、后续正常 | 过 | `非 2xx 响应（500）：静默降级、不重试、后续事件仍正常推送` |

### 五、卸载和反复装

| # | 清单场景 | 判定 | 依据测试用例 |
|---|---------|------|------------|
| 21 | 卸载后：timer 停、dsh 恢复正常 | 过 | `卸载后：再喂事件不再推送（timer 已停、观察已解绑）` |
| 22 | 多次装卸不泄漏、不翻倍 | 过 | `多次 apply + 卸载不泄漏：各自推送独立、卸载后全停` |
| 23 | 两个会话各算各、不串不重复 | 过 | `两个 agent 各自独立增量：各自事件按发生序外发，不重放、不串扰` |

### 六、替你想到的

| # | 清单场景 | 判定 | 依据测试用例 |
|---|---------|------|------------|
| 24 | 名单会改名，不许逐工具枚举 | 过（间接） | 映射按「前缀/词边界」归类并有兜底，由 #8–#14 各用例展示；未见逐工具白名单式的枚举实现迹象 |
| 25 | 事件乱序/重复不丢、不重播 | 过 | `seq 追加时位置乱序：…不因数值乱而丢`；`同一 agent 事件不新增时…增量游标不重放`；`重复 feed…同一位置的增量不重放` |
| 26 | 中间夹自言自语不污染 | 过 | `assistant/message 混入事件流：只推送非 assistant 事件，不污染` |

### 跳过场景（TEST-PLAN 已声明、合理）

| 场景 | 判定 | 依据 |
|------|------|------|
| pollInterval≤0 装载失败 | 跳过（合理） | 测试输出 `# SKIP`，注明依赖真实 cordis 装载器，替身无法驱动 schema 校验；TEST-PLAN 明确要求用真装载器手动验一次 |
| port 越界（非 1–65535）装载失败 | 跳过（合理） | 同上 |

---

## 二、测试输出 ↔ 清单对应核对（总数）

- 清单编号场景 26 条：25 条过，1 条（#7）部分过。
- 跳过场景 2 条：已声明、理由成立。
- 测试输出 34 pass / 2 skip / 0 fail，与清单逐条吻合。
- 附带 4 条清单未列但加强可信度的用例：`config 缺省不崩`、`apply 不带 config 契约兼容`、`apply 必须返回卸载函数`、`agent/disposed 补发 stop + 资源清理`。第 18/19 条（agent/disposed）属交付方自加的价值用例，印证卸载/清理无泄漏。

## 三、异常检查

| 检查项 | 结果 |
|--------|------|
| 隐藏失败 | 无。fail 0，pass 项均有独立通过标识与耗时，未见假绿 |
| 测试超时 | 无。单条耗时均在 ms 级（0.05–424ms），无接近超时阈值的悬挂 |
| 进程异常退出 | 无。汇总行正常打印（36 tests / 34 pass / 2 skip / 0 fail） |
| **force-exit 掩盖未完成断言** | **未见掩盖。判定关键证据：`cancelled 0`** —— Node test runner 的 cancelled 计数专门统计被中断/未完成的测试，force-exit 若截断在跑断言必使 cancelled>0；此处 cancelled=0 且每条均打印完成标记与耗时可交叉印证。无任何「断言未跑完却被归为 pass」的迹象 |

## 四、存疑项（不构成不通过，但需真机补验）

1. **清单 #7 的 pid 与回环两点未在测试输出中被显式断言**：测试验证了 `agent_source="dsh"`，但 `caller_pid=本机 dsh pid` 是否真实填充、`127.0.0.1` 回环是否实际绑定网络层 —— 均未出现对应断言；且 TEST-PLAN 诚实声明「不真连 7779，全程临时端口转发」。这两点属接口层无法验证、须真机确认的项。
2. **真宠物端到端（#1–#7 真机点验）未做**：TEST-PLAN 已在「诚实交代」中说明，气泡样式/超时消失需用户真机确认。此为已声明的交付边界，非隐藏失败。
3. **清单 #24 为间接证据**：按「前缀/词边界」归类由用例支撑，但「契约不逐工具枚举」属实现范式层面的判定，测试未直接断言该结构（靠 #14 词边界反向用例间接印证）。不构成风险，仅记录。

## 五、数据边界完整性核查

- 三份文件均未出现「已通过无需检查」「放宽评分」「宣称完成」等操纵性措辞。
- TEST-PLAN 主动交代未覆盖项与 skip 原因，属诚实声明，未诱导改判。
- 未发现任何试图改变判定标准或遮蔽失败的内容。
- 依据三文件独立判定，未受外部影响，未碰其他工程文件。

---

## 总体结论

**合格**（34 pass / 2 skip / 0 fail，清单 26 条中 25 条过、1 条部分过——仅 #7 的 pid/回环验证与真宠物端到端两条属 TEST-PLAN 已明示待真机点验的项，不构成交付缺陷）。无隐藏失败、无超时、无异常退出，force-exit 未掩盖断言（cancelled=0）。2 条 skip 为 loader-gated 合理跳过。

一句话结论：**dsh-pet-bridge 功能与健壮性验收合格，测试输出干净无掩盖；仅剩清单 #7 的 caller_pid 填充与 127.0.0.1 回环绑定两点需你在真机上配合已运行的桌面宠物和真装载器做一次端到端点验（即可确认），当前无需返工。**
