# SM 18 条修复验收报告（2026-08-17，opus 裁判盲判；主会话代为落盘）

> 输入：REVIEW-BLIND-20260817.md + FIX-TEST-PLAN.md + test-output.txt（主会话干净复跑）。
> 锁定核查：tests/ 下唯一改动 = tests/unit/sessionRowMatch.unit.test.js（冲突 1 授权退役项）。

## 总体结论：合格（无打回项）

## 逐条判定

| 条目 | 判定 | 依据 |
|---|---|---|
| F1 归档写 Promise 未 await | 过 | 5 红转绿 + 4 相邻护栏绿 |
| F2 行→id 错绑（可见性/行序/模板词） | 过 | 5 红 + 5 护栏绿；退役文件即授权项；unit 127/0fail/0skip 证明是改写非挂 skip |
| F3 smRestore 无 UI 调用点 | 过（结构断言口径） | 2 红 + 2 护栏转绿 |
| H1 绕 workspaceRegistry 缓存 | 过（按计划口径：标注+缓解） | 1 红 + 1 护栏转绿；刷新回滚需人工验证（见待补） |
| H2 stale read 丢更新 | 过 | 2 红 + 1 护栏转绿 |
| H3 empty() 门槛形同虚设 | 过 | 2 红（.DS_Store/普通目录误删）+ 2 护栏转绿 |
| H4 名单全等 + 无 realpath | 过 | 2 红 + 5 护栏（含 tmpdir 豁免红线）转绿 |
| M1 restore 不校验边界/结构 | 过 | 3 红 + 2 护栏转绿；拒绝码落在允许集合 |
| M2 无视 DSH_HOME | 过 | 1 红 + 2 护栏转绿 |
| M3 marker 只认 .zstd | 过 | 1 红 + 2 护栏转绿 |
| M4 域未开=没归档 | 过 | 1 红 + 2 护栏转绿 |
| M5 对账漂移（幽灵行+双页） | 过（双页为结构断言） | 2 红 + 2 护栏转绿 |
| L1 回滚删既有 record | 过 | 1 红 + 1 护栏转绿 |
| L2 deadline 语义 | 过 | 2 红 + 1 护栏转绿 |
| L3 按钮 id 冻结 | 过（结构断言） | 1 红 + 1 护栏转绿 |
| L4 全量重扫 | 过 | 1 红转绿（≤4×N byId） |
| L5 ESM require.resolve | 过 | 1 红 + 1 护栏转绿 |
| L6 gitignore/测试/脚本矛盾 | 过（强旁证） | 2 红转绿；三套既有测试从"跑不起来"到 261 全绿即行为级证据 |
| W1 Windows 系统目录放行 | 过 | 5 红 + 2 护栏（含 Windows 默认回收站放行红线）转绿 |
| W2 DSH_HOME Windows 失效 | 过 | 1 红 + 1 护栏（path.sep+绝对）转绿 |
| W3 字面 / 拼接护栏 | 过 | 保持绿，修复未新增违规 |
| W4 保留名 + 大小写碰撞 | 过 | 2 红 + 2 护栏转绿；W4 相邻用例显式 ✔（真跑在大小写不敏感卷，非运行时 skip） |
| W-真机 ×3 / browser-e2e ×6 | 合法 skip | 与 FIX-TEST-PLAN 声明逐字对应 |

## 账目核对

regression 92 = 承诺 92；pass 83 = 43 红转绿 + 40 护栏保持；fail 0；skip 9 = 6 e2e + 3 W-真机。**无用例消失**。unit 127 / integration 69 / acceptance 65 全 0 fail（无修前基线，作为后续基线记录）。

## 待补（不构成打回，关单前记录）

1. test-output 仅保留 4 条 case 名，逐条判定为"锁定+计数"闭环推断；后续重跑建议 spec reporter 全量落盘。
2. 三套既有测试无修前基线（修前跑不起来），本次 127/69/65 记为基线。
3. 计划自认未覆盖：6 条浏览器 e2e（F2 拖拽序 / F3 点击恢复 / L3 节点复用 / M5 双页 / H1 刷新回滚）、H1 的 2 分钟人工验证（取消归档→刷新→看是否回滚）、Windows 真机 3 条、Windows 文件占用（EBUSY/EPERM）零覆盖——已知风险而非未修。
4. lock check 段未记录产生命令，建议后续附命令行。

## 注入/放水排查

三份输入未发现试图改变判定标准的文字；FIX-TEST-PLAN 主动列未覆盖清单，属收紧非放宽。
